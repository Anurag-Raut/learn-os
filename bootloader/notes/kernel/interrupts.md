There are two types of interruptts 
actually 3
1) hardware interrupts  - these are also caleed as IRQs 
2) CPU exceptions - these are generated by CPU - for ex- seg fault , page fault
3) Software interrupts - basically syscalls , which interrupts the normal flow and ask for the control

PIC - i forget the fullform
but itst as chip on the hardware whose specific job is to collect hardware interrups from devices and dispatch to CPU

other interrups happend on cpu itself

Since the hardware interrupst number overlap witht he cpu expections 
0-31 is reserved by intel for cpu interrupts 
and irqs startt from 8

so what we do is PIC remapping , just like a hashtable in existing in PIC

